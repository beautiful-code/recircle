#book-show
  = div_for(@book,class: "container") do
    %h2= @book.name
    .row
      .col-sm-9.col-md-9
        = image_tag(@book.image,class: "img-responsive",alt: @book.name)
      .col-sm-3.col-md-3
        %p
          Owned By: #{link_to @user.name.capitalize,@user}
        -if current_user!=@book.user
          = button_to "Request Book",user_requests_path(user_id: current_user.id,request: {book_id: @book.id}),class: request_button_class
