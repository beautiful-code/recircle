- @notifications.each do |notification|
  %li
    = message(notification)
    = button_to read_user_notification_path(user_id:current_user,id:notification.id), method: :patch do
      %i.glyphicon.glyphicon-remove

