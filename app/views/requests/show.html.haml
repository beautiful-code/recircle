%h1
  Requests
  - @books.each do |book|
    - if book.unanswered_requests.count != 0
      %h4= link_to book.name,[current_user, book]
      - book.unanswered_requests.each do |request|
        %p
          Requested By #{(link_to request.user.name,request.user)}
        = button_to "Accept",accept_request_user_path(user_id: current_user.id,id: request.id),method: :patch,class: "btn btn-primary"
        = button_to "Decline",decline_request_user_path(user_id: current_user.id,id: request.id),method: :patch,class: "btn btn-primary"
