<div class="container">
  <% @books.each do |book|  %>
    <div class="center-block">
      <h2><%= book.name %></h2>
      <%= link_to  image_tag(book.image,class: "img-responsive",alt: book.name),user_book_path(current_user,id: book.id) %>
    </div>
  <% end %>
</div>
