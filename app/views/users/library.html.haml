.container
  - @books.each do |book|
    .center-block
      %h2= book.name
      = link_to  image_tag(book.image,class: "img-responsive",alt: book.name),user_book_path(user_id: book.user.id,id: book.id)
      = link_to "Edit",edit_user_book_path(user_id: current_user.id,id: book.id),class: "btn btn-primary"
